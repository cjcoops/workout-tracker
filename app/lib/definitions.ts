export type Program = {
  id: string;
  name: string;
};

export type Day = {
  id: string;
  day_name: string;
  program_name: string;
};

export type Workout = {
  id: string;
  name: string;
};

export type Exercise = {
  id: string;
  name: string;
  description: string;
};
